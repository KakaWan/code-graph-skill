---
name: code-graph
description: "项目结构图系统：哈希增量生成/更新模块-文件-函数调用图并存为 .code-graph/，按关键词检索小子图代替读源码。开始修改代码、理解跨文件依赖、定位函数/模块、会话恢复时自动调用。纯本地，无网络无第三方依赖。"
---

# Code Graph

用最小的 token 理解项目结构：`update.py` 增量生成模块-文件-函数调用图，`query.py` 按关键词返回小子图。**检索图代替读源码**，只在图信息不足时才读代码。

## 文件布局

```
.code-graph/
├── manifest.json      # 内容哈希库（sha256），增量检测核心
├── graph.json         # 完整图：modules / files / symbols / edges
├── code-graph.html    # 自包含交互式可视化（数据/样式/脚本全内联，双击即开）
├── index.md           # 全局索引（小），定位模块用
└── modules/<id>.md    # 模块分片：函数签名 + 调用/被调用 + 职责摘要
```

**所有生成物一律写入项目 `.code-graph/` 目录，绝不写到项目根或其他位置**——这是本 skill 的默认硬性约定（`update.py` 与 `render_html.py` 均已遵守，独立调用时同样适用）。

## 何时调用

- 会话开始或恢复时：先更新图并读 `index.md`（了解项目结构，替代扫描源码）。
- 接到修改/理解任务时：`query.py` 检索相关符号，读小子图 + 模块分片。
- 只读图，不读源码——除非图信息不足（签名/调用链无法回答需求时再按需读目标文件）。

## 更新协议（调用时自动增量）

1. 运行更新脚本（Windows 用 `python`，其他用 `python3`）。skill 已全局部署，直接引用全局路径：

   ```bash
   python ~/.claude/skills/code-graph/scripts/update.py --root <项目根>
   # Windows: python %USERPROFILE%\.claude\skills\code-graph\scripts\update.py --root <项目根>
   ```

2. 脚本输出 JSON 报告：`changed`（新增/修改文件）、`deleted`（删除文件）、模块/符号统计；并自动在 `.code-graph/` 内重新生成 `code-graph.html`（`htmlViewer` 字段为路径，可用 `--no-html` 跳过）。
   - 报告基于内容哈希（sha256），git pull 同步的他人修改也会被捕获。
3. 若 `changed` 非空：**读这些变化文件**（通常很少），把每个文件/模块的职责摘要（一两句中文）写入对应 `.code-graph/modules/<id>.md`：
   - 模块职责 → 该文件 `Summary:` 行（替换 `<!-- AI: fill ... -->` 占位）。
   - 关键函数 → 函数段 `职责:` 行（可选，首次全量只补模块级摘要，函数级按需补）。
   - 首次全量（全部文件都是 changed）时：基于文件名/符号列表为每个模块写摘要，不必逐个读文件；需要理解才读。
4. 若 `changed` 为空：图已最新，直接用。

## 检索协议

1. 定位：`python ~/.claude/skills/code-graph/scripts/query.py --root <项目根> <关键词>`
   输出命中符号：签名、文件、行号、调用/被调用、所属模块分片路径。
2. 细读：打开 `.code-graph/modules/<id>.md` 对应段（签名/调用链/职责摘要）。
3. 兜底：以上仍不足时，才读 `file:` 标注的源码文件（定位精确，局部小读）。

其他查询：
- 按文件：`query.py --root <根> --file <相对路径>`
- 机器可读：`query.py --root <根> <关键词> --json`

## HTML 可视化（code-graph.html）

`update.py` 每次运行后自动在 `.code-graph/` 内生成**自包含**的 `code-graph.html`（数据/样式/脚本全部内联，无网络无第三方依赖，file:// 直接打开），展示最新图：

- 顶部工具栏切换 **模块图 / 文件图 / 类图 / 函数图** 四种视图（模块图=import 级依赖，文件/类/函数图=调用级依赖）。
- **点击节点** → 右侧信息面板显示详情：函数（签名、KDoc/文档注释、文件:行号、调用者/被调用者，可点击跳转）、文件（符号列表、跨文件调用）、类（方法、注释、类间引用）、模块（依赖/被依赖、文件列表、AI 摘要）。
- 节点**按所属模块自动着色**（同模块同色、模块间不同色——颜色只用于分类识别依赖簇）；**节点大小反映关联数量**（关联越多节点越大，3~17px 平方根缩放）；布局为**区域化**，**位置按引用关系分堆（与颜色无关）**：主连通组件构成主环，从高连接枢纽（hub，按组件规模自适应的度数阈值）出发多源扩散，关系紧密的节点聚成**关系簇**沿环均布（大簇小簇交错），簇内按关联数同心环分层（hub 居中、低关联靠外）；**链式尾节点不单独分区**——hub 扩散被星桥阻断的链尾（如 `deleteByIds`→`deleteEmailHistory` 这类多段链）按相邻边数自动并入其上方多关联节点所在的区（星岛或簇），只有真正无邻接区的孤立子图才保留独立小簇；**"仅一个关联"的节点（degree=1）不占环位**，以**星形放射状**紧贴其唯一邻居环绕排列（扇半径随扇内叶子数增长，保证同扇叶子互不重叠，基准角由节点名哈希决定）；**脱离主组件的小型连通组件**（含 **hub 星**：高连接中心节点 + 仅与之相连的少量叶节点）自动从主环**剥离为独立区域**，在主环外围按角度均布（星岛 hub 居中、叶子围绕，预留叶子伸展空间）；**"无关联"节点（in=0 且 out=0，无任何连边）默认显示并单独成堆放在最外围独立区**——只要有任何一条边（无论引用别人还是被引用）就留在簇环或组件区域中（"孤立节点"开关可隐藏）；
- **弹性拉近默认关闭**（布局纯静态、拖拽不引起连锁抖动，工具栏可开启弹簧微调）——**弹簧按分区进行**：只对**同一分区内**的边生效（每个关系簇/岛屿内部各自收紧到理想距离），跨分区边（簇↔簇、簇↔岛屿）不产生拉力，分区之间互不拉扯；**弹簧只"拉"不"推"**（超过理想距离 K 的边沿线收缩到 K，K≈布局天然间距，低于 K 的边保持不动——避免把簇径向压扁），长边（>2.2K）整体忽略，开启时前 30 帧力从 0 平滑爬升（不突跳）；**放射叶子与岛屿成员和其中心节点组成刚体**——开启弹簧或拖拽时整组跟随中心节点平移（拖离单个叶子会使其脱离组合、可自由放置）；叶子/岛屿成员参与硬核防重叠：静态布局零重叠，弹簧/拖拽下被锁定的刚体成员把重叠推力**代理给其中心节点**（整组让位），布局始终无重叠（收敛后残留仅亚像素级）。
- **滚轮缩放**（以鼠标为中心）、**拖拽空白平移**、**拖拽节点重排**、**双击节点放大聚焦**；"重新布局"重跑力导向布局，"适应窗口"复位。
- 搜索框按名称/路径跨视图检索跳转；边为直线线段，可开关方向箭头；"孤立节点"开关可隐藏无连边节点（默认显示——它们放在独立 pile，见上文）。
- 工具栏三个勾选项（箭头 / 孤立节点 / 弹性拉近）鼠标悬停均显示用途说明（原生 tooltip）。
- Canvas 渲染 + 网格加速力导向模拟，数千节点可流畅交互。

手动重新生成（不重新扫描源码）：`python %USERPROFILE%\.claude\skills\code-graph\scripts\render_html.py --root <项目根>`（同样输出到 `.code-graph/` 内）。整个 `.code-graph/` 是生成物目录，建议整体加入 `.gitignore`。

## 修改代码后

- 本 skill 下次调用时自动哈希检测并增量更新，无需手动操作。
- 完成较大改动后可立即运行 `update.py` 同步图，避免会话后续基于旧图工作。

## 限制（重要，避免误导）

- 调用关系是**词法级静态匹配**（函数体内标识符 ↔ 符号表）：多态分发、反射、动态 import、跨进程调用检测不到。**同名函数（重载）会尽力消歧**：`prefix.method` 调用点按前缀的静态类型过滤目标（Kotlin 局部变量/构造参数显式类型、`val x = db.xxxDao()` 工厂名推断、`as` 强转推断、`val x = container.repo` 链式属性类型跨文件传播）；仍无法解析时退化为"同文件优先，否则全连"。同名 `insert` 类高频词在常见手写 DI（AppContainer 注入）模式中可精确区分到单个 DAO/Repository。图标注为"近似"，需要精确路径时读源码确认。
- Kotlin 的属性初始化器 / `init {}` 块中的调用（如 `val flow = repo.observe().stateIn(...)`）被归因到**所属类**（`<类名>.__property__` 伪调用者），在面板"调用者"中可见；函数图本身只画函数→函数边，因此仅被属性/init 引用的函数在函数图中仍显示为"无引用"。属性初始化器内部的函数**声明行**（如匿名对象里的 `override fun migrate(...)`）不会产生假调用边。
- **接口/抽象方法（无函数体）不产生任何调用边**——DAO 接口、抽象方法、系统回调（`onStartCommand`、`onLocationChanged` 等）的真实调用方在图外（Room 代理、Android 框架），显示为"无引用"属正常。
- 模块级 import 依赖（模块图）仅对基于 package 的语言（Kotlin/Java）解析；其他语言（Python/TS/Go 等）模块图只有节点，无边。
- 解析器覆盖 Kotlin / Java / Python / TypeScript(含 JS) / Go 的类与函数；其他语言（C/C++/Rust/Swift/Ruby/PHP/SQL 等）退化为**文件级结构**（无符号无调用链）。
- 匿名函数/lambda 不单独建符号（归入宿主函数）。
- 图是快照：语义摘要由 AI 补充，可能与代码漂移；`update.py` 重建时结构部分自动刷新，摘要保留（在 modules/*.md 中手动维护）。

## 即插即用

本 skill 已**全局部署**在 `~/.claude/skills/code-graph/`（Windows: `%USERPROFILE%\.claude\skills\code-graph\`），对所有项目自动可用，无需每个项目复制——这与项目代码完全无关，是独立的通用基础设施。唯一要求：Python 3.8+（系统自带，无第三方包）。

图生成物 `.code-graph/` 存于各项目根，是否提交版本控制由项目自行决定。如某项目需要独立副本（如团队共享或离线环境），把该目录复制到项目 `.claude/skills/` 下即可。
